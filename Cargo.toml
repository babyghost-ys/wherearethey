[package]
name = "wherearethey"
version = "0.1.0"
edition = "2024"
description = "Find where your CLI tools were installed from"
license = "GPL-3.0-only"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
